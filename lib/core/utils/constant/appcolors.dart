import 'package:flutter/material.dart';

/// Centralizes commonly used colors so the palette is easy to update later.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF1E6DB7);
  static const Color background = Color(0xFFF9FCFF);
  static const Color secondary = Color(0xFF272F41);

  static const Color titletext = Color(0xFF006CE0);
  static const Color textPrimary = Color(0xFF181E2F);
  static const Color secondaryText = Color(0xFF272F41);
  static const Color border = Color(0xFFE4E7EC);
}
